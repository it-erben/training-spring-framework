# In-Memory-H2 wie in Modul 03: Die Datenbank lebt nur so lange wie die JVM —
# im Integrationstest faehrt sie mit dem Kontext hoch und wieder runter.
spring.datasource.url=jdbc:h2:mem:bookstore
# Hibernate erzeugt das Schema beim Start aus den Entities. Nur fuer Demos
# und Tests geeignet (siehe Modul 03).
spring.jpa.hibernate.ddl-auto=create-drop
